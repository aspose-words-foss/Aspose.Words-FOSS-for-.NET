This is a "celler" module. It creates a "grid" for a table. 
This grid is used when exporting to HTML, ODT and old PDF.

We have newer "grid" code that is used for writing to DOCX and also for layout. It was decided to write 
new code separately because this old celler does several other things include some calculations with borders.
I can later check if the two modules can be combined into one.
