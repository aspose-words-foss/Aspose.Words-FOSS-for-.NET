// Copyright (c) 2001-2026 Aspose Pty Ltd. All Rights Reserved.
// 02/09/2013 by Ivan Lyagin

// 
// This source code was auto-generated by Aspose.CsClassTransformer.ConsoleApp, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null.
// Do not change the file manually as your changes will be lost during next auto-generation session.
// 

using System;
using Aspose.Collections;
using NUnit.Framework;

namespace Aspose.Tests.Collections.Auto
{
    /// <summary>
    /// Tests IntStack class' functionality.
    /// </summary>
    /// <remarks>
    /// Every individual test of this class corresponds to a IntStack member.
    /// See the corresponding IntStack members' descriptions for any additional information.
    /// </remarks>
    [TestFixture]
    public class TestIntStack
    {
        [Test]
        public void TestClear()
        {
            IntStack stack = new IntStack();
            stack.Push(LastItem);
            stack.Clear();
            Assert.That(stack.Count, Is.EqualTo(0));
        }

        [Test]
        public void TestContains()
        {
            IntStack stack = new IntStack();
            stack.Push(LastItem);
            Assert.That(stack.Contains(LastItem), Is.True);
            Assert.That(stack.Contains(gArray[0]), Is.False);
        }

        [Test]
        public void TestCopyTo()
        {
            int[] array = new int[gArray.Length];
            IntStack stack = new IntStack(gArray);
            stack.CopyTo(array, 0);
            Assert.That(array, Is.EqualTo(gReversedArray));
        }

        [Test, ExpectedException(typeof(ArgumentNullException))]
        public void TestCopyToNullArray()
        {
            IntStack stack = new IntStack(gArray);
            stack.CopyTo(null, 0);
        }

        [Test, ExpectedException(typeof(ArgumentOutOfRangeException))]
        public void TestCopyToNegativeArrayIndex()
        {
            int[] array = new int[gArray.Length];
            IntStack stack = new IntStack(gArray);
            stack.CopyTo(array, -1);
        }

        [Test, ExpectedException(typeof(ArgumentException))]
        public void TestCopyToInvalidArrayRange()
        {
            int[] array = new int[gArray.Length - 1];
            IntStack stack = new IntStack(gArray);
            stack.CopyTo(array, 0);
        }

        [Test]
        public void TestPeek()
        {
            IntStack stack = new IntStack(gArray);
            Assert.That(stack.Peek(), Is.EqualTo(LastItem));
            Assert.That(stack.Count, Is.EqualTo(gArray.Length));
        }

        [Test, ExpectedException(typeof(InvalidOperationException))]
        public void TestPeekEmpty()
        {
            IntStack stack = new IntStack();
            stack.Peek();
        }

        [Test]
        public void TestPop()
        {
            IntStack stack = new IntStack(gArray);
            Assert.That(stack.Pop(), Is.EqualTo(LastItem));
            Assert.That(stack.Count, Is.EqualTo(gArray.Length - 1));
        }

        [Test, ExpectedException(typeof(InvalidOperationException))]
        public void TestPopEmpty()
        {
            IntStack stack = new IntStack();
            stack.Pop();
        }

        [Test]
        public void TestPush()
        {
            IntStack stack = new IntStack();
            stack.Push(LastItem);
            Assert.That(stack.Count, Is.EqualTo(1));
            Assert.That(stack.Peek(), Is.EqualTo(LastItem));
        }

        [Test]
        public void ToArray()
        {
            IntStack stack = new IntStack(gArray);
            Assert.That(stack.ToArray(), Is.EqualTo(gReversedArray));
        }

        [Test]
        public void TestCount()
        {
            IntStack stack = new IntStack();
            Assert.That(stack.Count, Is.EqualTo(0));

            stack.Push(LastItem);
            Assert.That(stack.Count, Is.EqualTo(1));

            stack.Push(LastItem);
            Assert.That(stack.Count, Is.EqualTo(2));

            stack.Pop();
            Assert.That(stack.Count, Is.EqualTo(1));

            stack.Clear();
            Assert.That(stack.Count, Is.EqualTo(0));
        }

        [Test]
        public void TestCopyCtors()
        {
            IntStack stack1 = new IntStack(gArray);
            Assert.That(stack1.ToArray(), Is.EqualTo(gReversedArray));

            IntStack stack2 = new IntStack(stack1);
            Assert.That(stack2.ToArray(), Is.EqualTo(gReversedArray));
        }

        [Test, ExpectedException(typeof(ArgumentNullException))]
        public void TestCopyCtorNullArray()
        {
            IntStack stack = new IntStack((int[])null);
            Assert.Fail(string.Format(string.Empty, stack)); // Force the compiler not to skip the previous instruction.
        }

        [Test, ExpectedException(typeof(ArgumentNullException))]
        public void TestCopyCtorNullStack()
        {
            IntStack stack = new IntStack((IntStack)null);
            Assert.Fail(string.Format(string.Empty, stack)); // Force the compiler not to skip the previous instruction.
        }

        /// <summary>
        /// Returns an array extensively used in this test.
        /// </summary>
        private static int[] GetArray()
        {
            return new int[] { 6, 4, 1, 2, 5, 3 };
        }

        /// <summary>
        /// Returns reversed copy of the test array.
        /// </summary>
        private static int[] GetReversedArray()
        {
            int[] array = (int[])GetArray().Clone();
            Array.Reverse(array);
            return array;
        }

        /// <summary>
        /// Gets the last item of the test array.
        /// </summary>
        private static int LastItem
        {
            get { return gArray[gArray.Length - 1]; }
        }

        // Calculate once, use multiple times.
        private static readonly int[] gArray = GetArray();
        private static readonly int[] gReversedArray = GetReversedArray();
    }
}
